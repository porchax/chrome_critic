{
  "name": "criticus",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "format": "biome format --write .",
    "lint": "biome check .",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "5.6.3"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  }
}
